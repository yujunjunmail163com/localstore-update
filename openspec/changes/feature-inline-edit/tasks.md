## 1. 基础设施与事件委托

- [x] 1.1 在表格容器上实现事件委托（event delegation），通过 event.target 判断点击的是 Key 列还是 Value 列
- [x] 1.2 实现模块级变量 currentEditCell 追踪当前编辑状态，确保同一时间只有一个单元格处于编辑模式
- [x] 1.3 实现通用的退出编辑函数：将 input/textarea 还原为文本节点，清理事件监听

## 2. Key 行内编辑

- [x] 2.1 实现点击 Key 单元格进入编辑模式：将文本替换为 input 元素，预填充原始值，自动聚焦并选中文本
- [x] 2.2 实现 Key 保存逻辑：调用 removeItem(旧Key) + setItem(新Key, 原Value) 完成 Key 重命名
- [x] 2.3 实现 Enter 键和失去焦点时自动保存
- [x] 2.4 实现 Escape 键取消编辑，恢复原始 Key
- [x] 2.5 实现空 Key 名称校验：拒绝保存空值并恢复原始名称
- [x] 2.6 实现 Key 重名校验：若新 Key 已存在则拒绝保存并提示用户

## 3. Value 行内编辑

- [x] 3.1 实现点击 Value 单元格直接进入编辑模式（取消原有编辑图标逻辑）
- [x] 3.2 根据 Value 内容自动选择编辑器类型：短文本用 input，含换行符用 textarea
- [x] 3.3 实现 textarea 高度自适应内容
- [x] 3.4 实现 Enter/Ctrl+Enter 和失去焦点时自动保存 Value
- [x] 3.5 实现 Escape 键取消编辑，恢复原始 Value

## 4. 编辑状态互斥

- [x] 4.1 实现编辑中点击另一个单元格时，当前编辑自动保存并退出，新单元格进入编辑状态

## 5. 样式适配

- [x] 5.1 编写 input/textarea 的深色主题样式，确保与现有 UI 风格一致
- [x] 5.2 实现编辑态的视觉反馈（边框高亮、背景色变化等）
